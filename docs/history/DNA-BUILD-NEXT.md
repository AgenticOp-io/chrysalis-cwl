# DNA build — next queue

**Path:** Rosetta → UT → DNA — [`ROSETTA-UT-PATH.md`](../language/ROSETTA-UT-PATH.md)  
**Tip:** **`1.0.9`** — foreach IR + else/else-if + page early-exit HTML  
**Scope:** [`CWL-LANGUAGE-SCOPE.md`](../language/CWL-LANGUAGE-SCOPE.md)

## CWL-owned

*None required.* Remaining depth is Convert/oracle authority:

- Opaque call evaluate (`g_verify_password`, DB `post`/`user` fetch)
- Foreach N-iteration HTML under simulate
- Browser island event execution
- Thin emit reverse of early-guards / foreach (optional Rosetta polish)

## Convert / Secure (not this lane)

| Sibling | Ask | Doc |
| --- | --- | --- |
| **Convert** | Tip pin **1.0.9**; sync fat `cwl-ingest` (`appendForeachBindings` + else/else-if lower); keep attachment-holes smoke green | [`CONVERT-GRAVITY-REQUESTED.md`](./CONVERT-GRAVITY-REQUESTED.md) |
| **Secure** | Tip pin **1.0.9** | [`SECURE-CUTOVER-REQUESTED.md`](./SECURE-CUTOVER-REQUESTED.md) |
