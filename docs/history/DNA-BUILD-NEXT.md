# DNA build — next queue

**Path:** Rosetta → UT → DNA — [`ROSETTA-UT-PATH.md`](../language/ROSETTA-UT-PATH.md)  
**Tip:** **`1.0.11`** — thin emit response chrome + effects + page-load  
**Scope:** [`CWL-LANGUAGE-SCOPE.md`](../language/CWL-LANGUAGE-SCOPE.md)

## CWL-owned

Thin emit Rosetta for projectable surfaces is largely closed (control + chrome + effects + load). Remaining language polish is optional:

- UI tree / island emit reverse (SSR markers only — no browser events)
- `test:runtime-cwl` rehome to language-gold
- Dual-mode `cwl-fmt` (tooling)

## Sibling wait (oracle / product)

| Item | Owner |
| --- | --- |
| Opaque `g_*` / DB evaluate | Convert |
| Foreach N-iteration HTML | Convert |
| Browser island event execution | Convert |
| Tip pin **1.0.11** | Convert + Secure |

Siblings verified synced through **1.0.10** (fat foreach/else + registry pin). Chase **1.0.11** after this publish.
