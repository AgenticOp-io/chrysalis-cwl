# Traffic-decides bar — build program

**Status:** open · coordinator 2026-08-21  
**Slogan:** **AI drafts. Recorded traffic decides.**  
**Do not:** invent customer soak traffic · invent EXTFMAP · Nest/LiveView/Flutter façades

## Bar (ship = all three)

| Gate | Owner | Proof token | Notes |
| --- | --- | --- | --- |
| **1. Propose ≠ dispose** | Convert | `TRAFFIC_DECIDES_CONVERT_OK` | Dispose Plane + verify-gated apply; LLM/IS may propose only |
| **2. Oracle / recorded replay** | Convert | same composite | Flagship / PHP oracle (or honest hole) before “converted” |
| **3. Helix shadow-ready** | Secure | `TRAFFIC_DECIDES_SECURE_OK` | `soak-preflight-smoke` + `live-match-smoke` (CWL ⊆ DNA when bridging) |
| **4. Live customer soak → enforce** | **Operator** | ops only | Real `SHADOW_LOG`; never synthetic production traffic |

## Marketing honesty

Public copy may say **“AI drafts. Traffic decides.”** as the bar. Prefer **recorded traffic** when describing Convert/oracle. Live Helix soak remains **operator** until a real shadow window exists.

## Convert ask

1. Add `hub:traffic-decides-bar-smoke` composing:
   - dispose-plane refuse-without-gate
   - verify-apply refuse without confirm + green gate
   - one real oracle product smoke (PHP flagship or existing green pair)
2. Package script + document in `docs/AI-ASSIST.md` / UT canon pointer.
3. Reply OUTBOX with `TRAFFIC_DECIDES_CONVERT_OK` + SHA.

## Secure ask

1. Add `npm run traffic-decides-bar-smoke` composing:
   - `soak-preflight-smoke` → `SOAK_PREFLIGHT_OK`
   - `live-match-smoke` → `LIVE_MATCH_OK` (tip pin ≥ 1.0.23)
2. Doc: bar ≠ customer soak; enforce still needs ops `SHADOW_LOG`.
3. Reply OUTBOX with `TRAFFIC_DECIDES_SECURE_OK` + SHA.

## Brand

`proof.html` states the bar and links Hub / Helix smokes. Slogan kept; overclaim of finished customer soak avoided.

## Stop condition

Convert + Secure tokens green → BOARD marks **traffic-decides-bar** closed (ops soak still residual).
