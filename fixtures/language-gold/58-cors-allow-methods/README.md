# 58 — cors.allow methods

RFC-0020 deepen (tip **1.0.50**): `cors.allow methods GET POST …` and optional
`origin` + `methods` composition. Bare `cors.allow` / origin-only forms unchanged.
Host enforces CORS — CWL does not invent a CORS engine.
