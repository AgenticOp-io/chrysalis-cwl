# `21-form-action` — RFC-0016

**Parseable now:** catalogued `hole hub-svelte:form-action;` plus a static HTML form shell.

**Not parseable / out of scope:** SvelteKit `actions` export syntax, progressive enhancement, CSRF modeling — remain honest holes per the RFC (no invented parser syntax).
