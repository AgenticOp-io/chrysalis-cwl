# `07-auth-effects` — RFC-0007

**runtime-ok** — handlers with declared `session.read` / `session.write` effects return authored literals under `simulateHandler` (no 501).

Does **not** claim a real session store or auth gate — only that effect wrappers stay conclusive for these golds.
