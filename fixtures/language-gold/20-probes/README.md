# `20-probes` — RFC-0015

**Parseable now:** `@page` + `query` + `return html`, and `@page` + `param` + `load` + HTML (same surfaces as production smoke probes).

**Not in this gold:** runtime HTTP assertions, injected `ctx.*`, or convert `hub-cwl-runtime-production-smoke` — those are consumer gates.
