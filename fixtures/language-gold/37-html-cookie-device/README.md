# `37-html-cookie-device` — HTML cookie / load interpolate

RFC-0014 deepen: bare identifiers in `return html` may bind `cookie` names and `load` keys (classified device token), not raw User-Agent strings.

No UA regex / media-query evaluate in CWL — keep that as Go/browser/hole work.
