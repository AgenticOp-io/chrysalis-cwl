# `36-layout-chrome` — layout chrome wrap

RFC-0029: `layout name { chrome html "…"; … }` + `layout name;` on `@page`.

Shared chrome HTML prefixes the page body at ingest. Layout headers/cookies/holes/islands merge onto the page. Not RFC-0011 route-merge imports.
