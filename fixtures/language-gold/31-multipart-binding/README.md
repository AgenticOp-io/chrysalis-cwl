# `31-multipart-binding` — RFC-0026

**runtime-ok** — multipart field/file part names lower like body bindings; simulate reads JSON body map (no invented disk upload middleware).

Does not claim a real `multipart/form-data` parser or file store.
