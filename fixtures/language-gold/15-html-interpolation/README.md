# `15-html-interpolation` — RFC-0014

**runtime-ok** — HTML template bindings for path/load names under simulate.

## Checks

```text
GET /docs/x → 200 <p>x: x</p>
GET /blog/y → 200 interpolated load fields + cwl-page-load script
```
