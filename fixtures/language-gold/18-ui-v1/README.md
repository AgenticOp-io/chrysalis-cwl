# `18-ui-v1` — RFC-0019

**runtime-ok** — UI islands (`client ui` / `data-cwl-island`) + load-bound text under simulate SSR.

Does **not** claim browser event execution — only HTML emission of island markers / actions.
