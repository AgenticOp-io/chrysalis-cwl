# `35-form-urlencoded` — native urlencoded form POST

**runtime-ok** — `use urlencoded;` + `body` bindings with `application/x-www-form-urlencoded`.

Proves form DNA without inventing Svelte `actions{}` or Nest façades. Complements JSON body gold `05` and multipart gold `31`.
