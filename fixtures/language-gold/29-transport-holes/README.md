# `29-transport-holes` — SSE / WebSocket / multipart stay catalogued holes

Not runtime-ok — honest holes until an RFC can lower transport genes without inventing runtimes.

| Route | Hole |
| --- | --- |
| `GET /events` | `unsupported:sse` |
| `GET /ws` | `unsupported:websocket` |
| `POST /upload` | `unsupported:multipart` |
