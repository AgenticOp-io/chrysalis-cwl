# `32-stream-sse` — RFC-0027

**runtime-ok** — `stream sse;` sets `text/event-stream` and returns a single JSON value under simulate.

Does not claim continuous EventSource / multi-event streaming runtimes.
