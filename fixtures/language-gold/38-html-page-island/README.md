# `38-html-page-island` — HTML + page-level island

RFC-0030: `return html` may coexist with a sibling `client ui "…"` on the same `@page`. Island is contract metadata (RFC-0019/0028), not a client runtime invent.
