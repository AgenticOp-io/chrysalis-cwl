# `33-ui-island-contracts` — RFC-0028

**runtime-ok** — named `client ui "signup"` serializes `data-cwl-island-id`; `on change` / `on click` stay metadata attributes.

Does not claim hydration, React/Svelte lowering, or WebSocket duplex.
